'use client';
import React from 'react'

const Register = () => {
  return (
    <div>
        <button onClick={()=>console.log('click')} >add sector</button>
        </div>
  )
}

export default Register